[gd_scene load_steps=2 format=3]

[ext_resource path="res://scripts/tests/TestLivesManager.gd" type="Script" id=1]
[ext_resource path="res://scripts/lives/LivesManager.gd" type="Script" id=2]

[node name="TestLivesManager" type="Node"]
script = ExtResource( 1 )

[node name="LivesManager" type="Node" parent="TestLivesManager"]
script = ExtResource( 2 )

[node name="InfoLabel" type="Label" parent="TestLivesManager"]
text = "LivesManager test scene. The LivesManager node is a child and will run its _ready() on scene start to reconcile regen. Inspect SaveData or listen to signals for verification."
margin_left = 8.0
margin_top = 8.0
margin_right = 632.0
margin_bottom = 28.0
